# Production runtime image — expects prebuilt dist/ next to this file.
# Used by install.sh / release tarball (not the multi-stage Dockerfile above).
FROM nginx:alpine
COPY dist/ /usr/share/nginx/html/
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
