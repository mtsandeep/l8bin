# Normalize all text files to LF in the repo and on checkout (binaries are
# auto-detected and left untouched).
* text=auto eol=lf

# SQL migrations were LF-normalized before the blanket rule; keep explicit.
**/*.sql text eol=lf
